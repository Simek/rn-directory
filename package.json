{
  "name": "rnd-cli",
  "version": "0.0.1",
  "bugs": {
    "url": "https://github.com/simek/rnd-cli/issues"
  },
  "license": "MIT",
  "repository": "github:simek/rnd-cli",
  "bin": {
    "rnd-cli": "./bin/cli.js"
  },
  "files": [
    "bin"
  ],
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "package": "bun build ./index.ts --outfile ./bin/cli.js --target bun",
    "prepublishOnly": "bun package",
    "lint": "oxlint --type-aware && oxfmt --check",
    "lint:fix": "oxlint --type-aware --fix && oxfmt"
  },
  "dependencies": {
    "@clack/prompts": "^0.11.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6",
    "oxfmt": "^0.26.0",
    "oxlint": "^1.41.0",
    "oxlint-tsgolint": "^0.11.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "bun": ">=1.3"
  }
}
